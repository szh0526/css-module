import React from 'react';
import ReactDOM from 'react-dom';
//引用同一css模块后,抽离的css不重复
// import icon from 'iconfont';
import App from 'appModuleJs';

ReactDOM.render( < App / > , document.body.appendChild(document.createElement('div')));